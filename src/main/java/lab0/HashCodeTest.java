package lab0;

public class HashCodeTest {

    public static void main(String[] args) {

        // We created object from Class HashCodeTest that we just created
        HashCodeTest hashtest = new HashCodeTest();
        Integer a = 4;
        System.out.println(hashtest.hashCode());

        // hashCode() will return the object's address in memory

    }
}
